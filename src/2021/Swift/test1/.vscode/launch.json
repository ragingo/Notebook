{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/swift_test1",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "make"
        }
    ]
}