@if(Request.IsAuthenticated){
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("ログオフ", "LogOff", "Account") ]</text>
}
else{
    @:[ @Html.ActionLink("ログオン", "LogOn", "Account") ]
}
